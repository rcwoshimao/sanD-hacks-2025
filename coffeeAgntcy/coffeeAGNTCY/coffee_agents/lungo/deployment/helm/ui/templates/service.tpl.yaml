apiVersion: v1
kind: Service
metadata:
  name:  {{ .Values.appName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app:  {{ .Values.appName }}
spec:
  {{- if eq .Values.environment "local"}}
  type: NodePort
  {{- end}}
  ports:
  - port: {{ .Values.service.port }}
    name: http
    {{- if eq .Values.environment "local"}}
    nodePort: 30083
    {{- end}}
  selector:
    app:  {{ .Values.appName }}